<% if is_stoped == true %>
  <%= link_to "ポーズかいじょ", adventures_unpause_path(quest_id: quest.id), method: :patch, remote: true %>
<% else %>
  <%= link_to "ポーズする", adventures_pause_path(quest_id: quest.id), method: :patch, remote: true %>
<% end %>